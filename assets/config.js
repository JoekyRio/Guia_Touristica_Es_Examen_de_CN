// ===== 同步配置（可选）=====
// 不填写则笔记只保存在本机浏览器（localStorage），换设备不同步。
// 要实现手机/电脑自动同步：按部署说明创建一个免费 Supabase 项目，
// 然后把项目的 URL 与 anon key 粘贴到下面两处引号内即可。
const SUPABASE_CONFIG = {
  url: "",        // 例如 "https://xxxxxxxxxxxx.supabase.co"
  anonKey: ""     // 例如 "eyJhbGciOiJIUzI1NiIs..."
};